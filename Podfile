# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GAAppUpdate' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GAAppUpdate
  pod 'EZSwiftExtensions', '~> 2.0' #拓展 标准类型和类应该如何快速工作。Swift标准库、Foundation和UIKit的有用扩展集合。
  pod 'Toast-Swift', '~> 5.0.1' #toast -Swift是一个Swift扩展，它将toast通知添加到UIView对象类。它的目的是简单、轻量级和易于使用。 大多数toast通知可以用一行代码触发。

end
